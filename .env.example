# ==========================================
# VULMINI ENVIRONMENT CONFIGURATION
# ==========================================
# Gemini генерирует .env из этого шаблона при первом развертывании.
# НИКОГДА не коммитьте .env в Git!

# ── Домен ──
DOMAIN_NAME=vulmini.wl.mk
LETSENCRYPT_EMAIL=admin@vulmini.wl.mk

# ── База Данных (MariaDB) ──
DB_NAME=vulmini_lms
DB_USER=vulmini_user
DB_PASSWORD=generate_secure_password_here
DB_ROOT_PASSWORD=generate_root_password_here

# ── Vultr API ──
VULTR_API_KEY=your_vultr_api_key_here
VULTR_REGION=tlv
VULTR_PLAN=vhf-2c-4gb
VULTR_PROD_INSTANCE_ID=

# ── Vultr Object Storage (S3 для медиа) ──
# Gemini заполнит эти данные после создания бакета через Vultr API
S3_ACCESS_KEY=placeholder
S3_SECRET_KEY=placeholder
S3_BUCKET_NAME=vulmini-media-bucket
S3_REGION=tlv1
S3_URL=https://tlv1.vultrobjects.com

# ── SSH (для MCP-сервера) ──
SSH_HOST=
SSH_PORT=22
SSH_USER=root
SSH_PRIVATE_KEY_PATH=~/.ssh/vulmini_rsa

# ── Системные лимиты ──
PHP_MEMORY_LIMIT=512M
UPLOAD_MAX_FILESIZE=2G

# ── WordPress Salts ──
# Генерируются автоматически при первом запуске через WP-CLI
# wp config shuffle-salts
